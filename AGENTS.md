# Project instructions

## 最優先: ゲーム進行を止めない

- このプロジェクトはゲーム実況に使うため、通常のゲーム進行が止まることを最もクリティカルな問題として扱う。
- OCR、朗読転写、実況生成などの回復可能な不一致や失敗だけを理由に、通常の文字送りを停止したりプロセスを終了したりしない。警告と記録を残し、妥当なフォールバックで進行を継続する。
- 再試行が必要な処理には上限または継続用フォールバックを設け、無期限に待機しない。
- 対象ウィンドウを特定できない場合や、続行するとゲーム外への入力・破壊的操作につながる場合は例外として停止してよい。

## 実装後レビューを必ず行う

- コードや設定を実装・修正した後は、完了報告の前に必ず変更差分をレビューする。
- レビューでは、ゲーム進行を止める経路、無期限待機、誤ったキー入力、既存データの破損・上書き、失敗時フォールバック、既存動作への影響を重点的に確認する。
- 問題を見つけた場合はその場で修正し、関連テストと全体テストを再実行したうえで、修正後の差分をもう一度レビューする。
